<div class="cover-confirm"></div>

<div class="container margin-top-negative">
  <div class="container mt-4">
    <div class="row">
      <div class="col-6 m-auto">
        <div class="flat-show-card">
          <h1>Congratulations!</h1>
          <h1>You're set to <strong><%= @flight.destination %></strong></h1>
          <br>
          <p>Date: <%= @flight.date %></p>
          <p>Departure time: <%= @flight.time.strftime("%H:%M") %></p>
          <p>Journey Duration: <%= @flight.duration %> hours</p>
          <p>Paid: £<%= @flight.price %></p>
          <%= link_to "Cancel booking", flights_path(@flight), class: "btn-cancel",
              data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to cancel this booking?" },
              class: "ms-3 text-danger" %>
          <%= link_to "Back to homepage", flights_path, class: "btn-home" %>
        </div>
      </div>
    </div>
  </div>
</div>
